export interface ApplicationType {
  id: string;
  application_name: string;
  version: string;
  pathToFile: string;
  pathToIcon: string;
  is_installed: boolean;
  created_at: string;
  updated_at: string;
}
